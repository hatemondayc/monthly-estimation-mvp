# ROADMAP — monthly-estimation-mvp

## 백로그 (우선순위 순)

---

### [TODO] 1차 추정에서 업데이트 버전 생성 (버전 복제)

**이유**: 현재는 1차 추정과 업데이트 버전을 별도로 처음부터 입력해야 한다. 1차 추정 행을 그대로 복사해서 업데이트 버전을 만들면 입력 공수가 크게 줄어든다.

**우선순위**: 높음

**수용 조건**:
- 1차 추정 버전에서 "업데이트 버전 생성" 버튼 클릭 시 행 전체를 복사해 업데이트 버전 생성
- 복사된 행은 독립 수정 가능 (원본 영향 없음)
- 이미 업데이트 버전이 존재하면 덮어쓸지 확인 후 진행

**상태**: TODO — PR #1 범위 제외, 다음 PR에서 구현

---

### [TODO] 버전 관리 UI (버전 생성/이름 변경/삭제)

**이유**: 현재 버전은 시드 또는 API를 통해서만 생성 가능. 웹 UI에서 버전을 추가·수정·삭제할 수 있어야 한다.

**우선순위**: 중간

**수용 조건**:
- 버전 생성 폼 (yearMonth, teamScope, roundType, roundLabel, memo)
- 버전 이름/메모 수정
- 버전 삭제 (연결 행 삭제 포함, 확인 다이얼로그)

**상태**: TODO

---

### [TODO] CSV 가져오기 (선택 사항)

**이유**: 기존 엑셀 데이터를 웹으로 빠르게 옮길 수 있으면 초기 정착 속도를 높일 수 있다.

**우선순위**: 낮음

**수용 조건**:
- 엑셀 내보내기 컬럼 순서와 동일한 CSV 형식 허용
- 유효하지 않은 행은 오류 행 목록으로 표시하고 나머지는 가져오기 진행

**상태**: TODO
